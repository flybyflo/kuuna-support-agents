FROM node:22-bookworm

WORKDIR /workspace

RUN apt-get update \
  && apt-get install -y --no-install-recommends docker.io \
  && rm -rf /var/lib/apt/lists/* \
  && corepack enable

CMD ["pnpm", "--filter", "@kuuna/backend-ts", "dev"]
