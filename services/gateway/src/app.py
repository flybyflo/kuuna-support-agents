"""Gateway process entry (scaffold).

This module is intentionally minimal for now. Integration detail:
- Neonize MessageEv should be mapped with `map_neonize_message_event`
- The mapped payload must include `raw_event` with full provider event content
"""
